#!/bin/bash
set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/../.."

cargo metadata --format-version 1 --no-deps |
  jq \
    '.packages | map({ name: .name, version: .version, "build-info": (.metadata."build-info" // {}) })'
