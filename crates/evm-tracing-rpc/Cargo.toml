[package]
name = "evm-tracing-rpc"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { workspace = true, features = ["derive"] }
sp-core = { workspace = true, features = ["std"] }
