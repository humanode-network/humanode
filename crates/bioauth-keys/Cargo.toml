[package]
name = "bioauth-keys"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
sp-application-crypto = { workspace = true }
sp-keystore = { workspace = true }
thiserror = { workspace = true }

[features]
default = []
