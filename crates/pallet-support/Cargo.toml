[package]
name = "pallet-support"
version = "0.1.0"
edition = "2021"

[dependencies]
frame-support = { workspace = true }

[features]
default = ["std"]
std = [
  "frame-support/std",
]
try-runtime = ["frame-support/try-runtime"]
